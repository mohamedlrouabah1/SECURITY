# when we launch the app the current path is the root of the project
DIR_TEMPLATES = "../templates"
DIR_STATICS = "../static"

# wich function used to compute otop 
VERSION_ALGO =    'perso' # 'pyotp' #  